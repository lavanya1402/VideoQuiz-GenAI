# ---------- Python ----------
__pycache__/
*.py[cod]
*.pyd
*.dll
*.so
*.egg-info/
dist/
build/
.eggs/
*.ipynb_checkpoints
*.pytest_cache/
pip-wheel-metadata/

# ---------- Virtual envs ----------
.venv/
venv/
env/

# ---------- Secrets / Config ----------
.env
.env.*
!.env.example
.streamlit/secrets.toml
# Keep config if you want (safe); comment out next line if you track it
# .streamlit/config.toml

# ---------- App data / temp ----------
uploads/
temp/
tmp/
*.log
*.cache
*.part
*.ytdl
*.aria2

# ---------- Captions / Transcripts (ephemeral) ----------
# Comment these out if you want to version them
*.vtt
*.srt

# ---------- Large media ----------
*.mp4
*.mov
*.mkv
*.webm
# allow tiny demo asset(s)
!assets/sample.mp4
# or a folder of tiny demos:
# !assets/samples/**

# ---------- Credentials you might export locally ----------
cookies.txt
cookies/*.txt

# ---------- OS / IDE ----------
.DS_Store
Thumbs.db
.vscode/
.idea/
*.code-workspace

# ---------- FFmpeg binaries (if you ever dropped them in repo by mistake) ----------
ffmpeg.exe
ffprobe.exe
